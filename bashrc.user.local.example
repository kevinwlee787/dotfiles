#!/usr/bin/env bash
# Copy to ~/.bashrc.user.local (or symlink it) for anything site-specific:
# internal tooling, private hostnames, per-machine paths. Gitignored.
#
# Example: regenerate compile_commands.json so clangd sees the current build
# graph, from any subdirectory of the workspace.
#
# compile_commands() {
#     local workspace_root
#     workspace_root=$(git rev-parse --show-toplevel) || return 1
#     your-build-tool --workspace "${workspace_root}" --languages cpp
# }
